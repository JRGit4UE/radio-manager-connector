
FTT
This is version 2 (with credentials stored outside in config (but not yet in docker secrets))

To re-create the project:

open a shell
cd into this directory and execute

npm install

run the example by executing

node index.js

